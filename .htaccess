# Mengaktifkan Mesin Rewrite
RewriteEngine On

# Menetapkan basis URL ke root domain
RewriteBase /

# Menetapkan halaman error kustom
ErrorDocument 404 /404.html

# KONDISI UTAMA:
# Aturan di bawah ini hanya berjalan jika yang diminta BUKAN file yang ada...
RewriteCond %{REQUEST_FILENAME} !-f
# ...DAN BUKAN direktori yang ada.
RewriteCond %{REQUEST_FILENAME} !-d

# ATURAN UTAMA:
# Arahkan SEMUA permintaan yang lolos dari kondisi di atas ke index.php
RewriteRule . /index.php [L]

# OPSIONAL: Aturan untuk Caching (biarkan saja jika sudah ada)
<IfModule mod_expires.c>
   ExpiresActive On
   ExpiresByType image/jpg "access 1 year"
   # ... sisa aturan cache Anda ...
</IfModule>